# This barebone is Uniwill, not Clevo. On this hardware the Clevo WMI interface
# is a stub (its methods return 0xffffffff) and does not drive the keyboard;
# blocking it prevents a boot-time race with the working uniwill path.
# Installed to /etc/modprobe.d/.
blacklist clevo_wmi
blacklist clevo_acpi
